{
  "name": "auth-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n db,api,web -c blue,yellow,green \"docker compose up\" \"cargo run --bin local\" \"pnpm --filter web dev\"",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "generate": "cargo run --bin export-openapi > openapi/openapi.json && pnpm --filter @ericminassian/auth generate"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "concurrently": "^9.1.2"
  },
  "packageManager": "pnpm@11.6.0"
}
